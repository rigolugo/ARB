# START_HERE

## Repository and public-data warning

Canonical repository: `rigolugo/ARB`.

**This repository is public.** Do not commit credentials, private keys, tokens, account secrets, private URLs, sensitive account data, local databases, or other private material.

## Safety defaults

- Gustavo is the sole project approval authority.
- Technical capability, repository access, credentials, tooling, or prior activity do not constitute authorization.
- Missing, omitted, malformed, unknown, ambiguous, stale, or conflicting capability fails closed.
- No venue access, credential use, funding, order activity, cancellation, paper/live trading, production write, or later phase is permitted unless the active Gustavo authorization explicitly permits it within `project_context/GUARDRAILS.md`.
- Demo and production remain structurally separated; risk tier never grants capability.

## Source-of-truth routing

Read `project_context/START_HERE.md` for the canonical read order and authority hierarchy.

Use:

- `project_context/GUARDRAILS.md` for permanent safety constraints;
- `project_context/PROJECT_AGENT_DISPATCH_AUTHORITY.md` for current-chat dispatch and same-scope correction rules;
- `project_context/PROJECT_STATE.md` for current accepted project state;
- `project_context/AGENT_ROLES.md` for standing role boundaries;
- `project_context/AUTHORIZATION_LOG.md` and `project_context/DECISION_LOG.md` for audit history; and
- `BROWSER_BRANCH_REPOSITORY_TRANSFER_WORKFLOW.md` for repository-transfer procedure.

A bounded exact Gustavo current-chat dispatch may itself be the operative task authorization. Authorization and decision logs are audit history: they need not pre-exist an already-authorized task, cannot retroactively authorize out-of-scope activity, and do not fill missing capabilities.

## Current state

Do not duplicate or infer the current project phase here. Read `project_context/PROJECT_STATE.md` and reverify any task-required canonical base directly against the repository before acting.
